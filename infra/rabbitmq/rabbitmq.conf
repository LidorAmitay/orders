## Disable the deprecated management_metrics_collection feature.
## Metrics are served via the Prometheus endpoint on port 15692 instead.
deprecated_features.permit.management_metrics_collection = false
